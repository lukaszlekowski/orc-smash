defaultProfile: audit
profiles:
  audit: { provider: claude, sessionStrategy: resume-per-skill }
  follow-up: { provider: opencode, sessionStrategy: resume-per-skill }
  implement: { provider: codex, sessionStrategy: resume-per-skill }
