# Model catalogues are maintained in config/providers/<provider>.yaml.
defaults:
  agent: opencode
  model: opencode-go/deepseek-v4-flash

timeouts:
  # claude / codex / agy are config-only (config > built-in 0; no env vars).
  claude: 0
  codex: 0
  agy: 0
